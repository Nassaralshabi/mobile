# 🔧 دليل صفحة الإعدادات - تطبيق فندق مارينا

## 📱 نظرة عامة
تم إنشاء صفحة إعدادات شاملة تتيح لك تخصيص جميع جوانب التطبيق، خاصة إعدادات الخادم والمزامنة.

## 🌐 **إعدادات الخادم**

### 1. **رابط الخادم**
- **الوصف**: الرابط الأساسي لـ API الخاص بخادم فندق مارينا
- **مطلوب**: نعم ✅
- **أمثلة**:
  ```
  http://localhost/marina-hotel/api          (للخادم المحلي)
  http://192.168.1.100/marina-hotel/api      (للشبكة المحلية)
  https://your-domain.com/marina-hotel/api   (للخادم العام)
  http://your-ip:8080/marina-hotel/api       (مع منفذ مخصص)
  ```

### 2. **مفتاح API**
- **الوصف**: مفتاح المصادقة للوصول للـ API
- **مطلوب**: اختياري
- **الاستخدام**: يُرسل في header كـ `Authorization: Bearer YOUR_API_KEY`

### 3. **بيانات المصادقة**
- **اسم المستخدم**: للمصادقة الأساسية
- **كلمة المرور**: للمصادقة الأساسية
- **مهلة الاتصال**: الوقت المسموح للاتصال (5-30 ثانية)

## 🔄 **إعدادات المزامنة**

### 1. **المزامنة التلقائية**
- **تفعيل/إلغاء**: المزامنة التلقائية مع الخادم
- **الفترة**: كل كم ثانية تتم المزامنة (15-300 ثانية)
- **المحاولات**: عدد المحاولات عند فشل المزامنة

### 2. **خيارات متقدمة**
- **WiFi فقط**: المزامنة عبر WiFi لتوفير البيانات
- **الوضع غير المتصل**: السماح بالعمل بدون إنترنت

## ⚙️ **إعدادات التطبيق**

### 1. **اللغة والمظهر**
- **اللغة**: العربية أو الإنجليزية
- **المظهر**: فاتح، داكن، أو تلقائي

### 2. **الإشعارات والتحليلات**
- **الإشعارات**: تفعيل إشعارات التطبيق
- **التحليلات**: إرسال بيانات الاستخدام (مجهولة)

## 🚀 **كيفية الاستخدام**

### في التطبيق المحمول:
1. افتح التطبيق
2. اضغط على أيقونة الإعدادات ⚙️ في شريط التطبيق
3. أدخل رابط الخادم الخاص بك
4. اختبر الاتصال
5. احفظ الإعدادات

### في صفحة الويب:
1. افتح ملف `server_settings.html`
2. أدخل إعدادات الخادم
3. اضغط "اختبار الاتصال"
4. احفظ الإعدادات

## 🔍 **اختبار الاتصال**

### ما يحدث عند الاختبار:
1. **إرسال طلب** إلى `{server_url}/test.php`
2. **التحقق من الاستجابة** (HTTP 200)
3. **عرض النتيجة**:
   - 🟢 **متصل**: الخادم يعمل بشكل طبيعي
   - 🟡 **جاري الاختبار**: انتظار الاستجابة
   - 🔴 **غير متصل**: فشل الاتصال أو انتهاء المهلة

### مشاكل شائعة وحلولها:

#### ❌ **"فشل الاتصال"**
- **السبب**: رابط خاطئ أو خادم غير متاح
- **الحل**: تحقق من الرابط وحالة الخادم

#### ⏰ **"انتهت مهلة الاتصال"**
- **السبب**: الخادم بطيء أو مشاكل في الشبكة
- **الحل**: زيادة مهلة الاتصال أو تحسين الشبكة

#### 🔒 **"غير مصرح"**
- **السبب**: مفتاح API خاطئ أو مفقود
- **الحل**: تحقق من مفتاح API أو بيانات المصادقة

## 💾 **حفظ واستعادة الإعدادات**

### الحفظ التلقائي:
- يتم حفظ الإعدادات تلقائياً في التخزين المحلي
- تبقى الإعدادات محفوظة حتى بعد إغلاق التطبيق

### النسخ الاحتياطي:
```json
{
  "server_settings": {
    "server_url": "http://your-server.com/api",
    "api_key": "your-api-key",
    "connection_timeout": 10
  },
  "sync_settings": {
    "auto_sync": true,
    "sync_interval": 30,
    "sync_wifi_only": false
  },
  "app_settings": {
    "language": "ar",
    "theme": "light",
    "enable_notifications": true
  }
}
```

## 🔧 **الإعدادات المتقدمة**

### للمطورين:
```dart
// تحديث إعدادات الخادم برمجياً
await SettingsService().updateServerSettings(
  serverUrl: 'http://your-server.com/api',
  apiKey: 'your-api-key',
);

// اختبار الاتصال
final isConnected = await ConnectivityService().testConnection();
```

### متغيرات البيئة:
```bash
# في ملف .env
MARINA_API_URL=http://localhost/marina-hotel/api
MARINA_API_KEY=your-api-key-here
MARINA_SYNC_INTERVAL=30
```

## 📊 **مراقبة الأداء**

### مؤشرات الأداء:
- **وقت الاستجابة**: < 2 ثانية للاتصال العادي
- **معدل النجاح**: > 95% للمزامنة
- **استهلاك البيانات**: ~ 1KB لكل عملية مزامنة

### التشخيص:
1. **شاشة حالة المزامنة**: عرض تفصيلي للحالة
2. **سجلات الأخطاء**: تتبع المشاكل
3. **إحصائيات الاستخدام**: مراقبة الأداء

## 🛡️ **الأمان والخصوصية**

### حماية البيانات:
- 🔐 **تشفير كلمات المرور**: محفوظة بشكل آمن
- 🛡️ **HTTPS مُوصى به**: للاتصالات الآمنة
- 🔒 **عدم تخزين بيانات حساسة**: في السجلات

### أفضل الممارسات:
1. **استخدم HTTPS** للخوادم العامة
2. **غيّر مفاتيح API** دورياً
3. **راقب سجلات الوصول** على الخادم
4. **فعّل المصادقة الثنائية** إن أمكن

## 🔄 **سيناريوهات الاستخدام**

### 1. **الإعداد الأولي**:
```
1. تثبيت التطبيق
2. فتح الإعدادات
3. إدخال رابط الخادم
4. اختبار الاتصال
5. حفظ الإعدادات
6. بدء الاستخدام
```

### 2. **تغيير الخادم**:
```
1. فتح الإعدادات
2. تحديث رابط الخادم
3. اختبار الاتصال الجديد
4. حفظ التغييرات
5. مزامنة البيانات
```

### 3. **استكشاف الأخطاء**:
```
1. فحص حالة الاتصال
2. مراجعة سجل الأخطاء
3. اختبار الاتصال يدوياً
4. تحديث الإعدادات حسب الحاجة
5. إعادة المحاولة
```

## 📞 **الدعم والمساعدة**

### مشاكل شائعة:

#### **التطبيق لا يتزامن**
- تحقق من رابط الخادم
- اختبر الاتصال يدوياً
- تأكد من تفعيل المزامنة التلقائية

#### **بطء في الاستجابة**
- زيادة مهلة الاتصال
- تحقق من سرعة الإنترنت
- قلل فترة المزامنة

#### **أخطاء المصادقة**
- تحقق من مفتاح API
- راجع بيانات المستخدم
- تأكد من صلاحيات الوصول

### للحصول على المساعدة:
1. راجع شاشة حالة المزامنة
2. تحقق من سجلات الأخطاء
3. جرب الإعدادات الافتراضية
4. تواصل مع فريق الدعم

---

## 🎉 **تهانينا!**

تم إنشاء نظام إعدادات متكامل يتيح لك:

✅ **تخصيص رابط الخادم** حسب احتياجاتك  
✅ **إعداد المزامنة** بمرونة كاملة  
✅ **اختبار الاتصال** قبل الحفظ  
✅ **حفظ الإعدادات** تلقائياً  
✅ **واجهة سهلة الاستخدام** باللغة العربية  

**التطبيق الآن قابل للتخصيص بالكامل ويعمل مع أي خادم!** 🚀

*تم تطويره بـ ❤️ لفندق مارينا*
